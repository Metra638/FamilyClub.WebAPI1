apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend        
  namespace: test
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: familyclub
    app.kubernetes.io/component: backend
  annotations:
    description: "ServiceAccount for backend pods to access secrets"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: backend-secret-reader
  namespace: test
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["dockerhub-secret", "backend-secrets"]
  verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: backend-secret-access
  namespace: test
subjects:
- kind: ServiceAccount
  name: backend
  namespace: test
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: backend-secret-reader
